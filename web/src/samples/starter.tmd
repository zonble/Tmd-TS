::SCORE::
** 小星星 (Twinkle Twinkle) **
!= 100 /* 速度：每分鐘 100 拍 (BPM) */
?= C   /* 調性：C 大調 */
<4/4>  /* 拍號：四四拍，每小節 4 拍 */

/*
 💡 歡迎來到 TMD Studio！(Timebase Mark Down)
 這是音樂製作人阿怪 (Chen, Chih-Han) 設計的純文字音樂標記語言：
 - 簡譜音符：1 2 3 4 5 6 7 (Do Re Mi Fa Sol La Si)
 - 高低八度：1^ (高音)、1_ (低音)
 - 延音線：- (延續上一拍時值)
 - 休止符：0 或 . (休止一拍)
 - 和弦標記：[1], [4], [5] 或 [C], [F], [G]
 
 💡 創作小技巧：
 - 網格時值轉換：在編輯器中「按滑鼠右鍵」或點選「工具」，可使用「細分/減半網格時值」
   自動幫你轉換 <4*> 與 <8*>（並自動縮放音符與延音時值）！
 - 🎤 哼唱寫歌：按滑鼠右鍵選擇「哼唱轉記為樂段」，對著麥克風哼唱旋律，
   系統內建的 AI 音高模型會自動幫你轉成 TMD 簡譜！
 
 點擊上方「▶ 試聽」即可聽見三軌合奏與曲式進行 (A -> B -> 升半音 -> A)！
*/

/*
 ====================================================
 A 段 (Verse / 主題)：一閃一閃亮晶晶，滿天都是小星星
 ====================================================
 TMD 格式為：段落:樂器@|小節偏移|{ ... }
 
 <4*>：網格設為四分音符 (每音 1 拍)，4/4 拍中每小節剛好 4 拍。
 <2*>：網格設為二分音符 (每格 2 拍)，每小節剛好填 2 個和弦。
 (💡 提示：在軌道內按右鍵，可隨時「細分網格」切換成 <8*> 寫更密集的音符)
*/
A:Lead@|0|{
    <4*>
    | 1 1 5 5 | 6 6 5 - |
    | 4 4 3 3 | 2 2 1 - |
}

A:Piano@|0|{
    <2*>
    | [1] [1] | [4] [1] |
    | [4] [1] | [5] [1] |
}

A:Bass@|0|{
    <4*>
    | 1_ - 1_ - | 4__ - 1_ - |
    | 4__ - 1_ - | 5__ - 1_ - |
}

/*
 ====================================================
 B 段 (Chorus / 轉折)：掛在天空放光明，好像許多小眼睛
 ====================================================
 同樣包含主旋律、鋼琴和弦、貝斯三軌：
*/
B:Lead@|0|{
    <4*>
    | 5 5 4 4 | 3 3 2 - |
    | 5 5 4 4 | 3 3 2 - |
}

B:Piano@|0|{
    <2*>
    | [1] [4] | [1] [5] |
    | [1] [4] | [1] [5] |
}

B:Bass@|0|{
    <4*>
    | 1_ - 4__ - | 1_ - 5__ - |
    | 1_ - 4__ - | 1_ - 5__ - |
}

/*
 ====================================================
 演奏流程宣告 (Playback Orders)
 ====================================================
 箭頭 -> 用來定義曲式進行順序。
 小星星是經典的 A-B-A 三段體。
 最酷的是：TMD 支援在流程中直接動態轉調！
 這裡在最後一段 A 之前插入 {?+1}（升半音轉 Db 大調），
 像流行歌最後副歌一樣推向高潮，完全不用重寫一次 A 段！
*/
-> A -> B -> {?+1} -> A ->#
